import React from 'react'


export default function Learn() {
  return <section>
    <ul>
      <li>Interactive hypertext narrative engine</li>

      <li>A story is a set of expositions within a given location and date-range</li>

      <li>Nouns that are recorded in the database are linked to other expositions containing the same</li>

      <li>Expositions are linked others that adjacent in time and space</li>
    </ul>
  </section>
}